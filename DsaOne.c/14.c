#include<stdio.h>
int main(){
    int n;
    printf("enter array size :");
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    int k;
    printf("enter kth element :");
    scanf("%d",&k);
    for(int i=0;i<n;i++){
        
    }
    printf("%d",arr[k]);

    
    return 0;
}